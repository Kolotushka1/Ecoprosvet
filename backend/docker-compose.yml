services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./media_server:/app  # Правильное указание volume для синхронизации кода
    ports:
      - "80:8000"
    networks:
      - django_network

networks:
  django_network: